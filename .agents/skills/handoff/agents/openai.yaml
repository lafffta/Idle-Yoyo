interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "What will the next session be used for?"
policy:
  allow_implicit_invocation: false
