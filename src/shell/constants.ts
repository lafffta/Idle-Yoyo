/** Provisional shell product assumptions, kept together so playtesting can tune them deliberately. */
export const PROVISIONAL_SHELL = {
  projectedNightSeconds: 8 * 60 * 60,
  meaningfulRestoredAbsenceSeconds: 60,
} as const;
